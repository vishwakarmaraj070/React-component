import React from "react";
import PropTypes from "prop-types";
import SaralLoader from "../component/SaralLoader";

const LoaderPages = props => {
  return (
    <div>
      <SaralLoader />
    </div>
  );
};

LoaderPages.propTypes = {};

export default LoaderPages;
